% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_validation.R
\name{plot_validation_scatter}
\alias{plot_validation_scatter}
\alias{plot_validate_flow_scatter}
\title{Plot validation scatterplots for raw, adjusted, and benchmark flows}
\usage{
plot_validation_scatter(
  residuals,
  comparisons = "adjusted_vs_benchmark",
  methods = NULL,
  method_col = "method",
  method_labels = NULL,
  difference_limits = NULL,
  difference_quantile = 0.95,
  white_band = 0.22,
  point_alpha = 0.45,
  point_size = 1.1,
  point_outline = TRUE,
  point_outline_colour = "#6B7280",
  point_stroke = 0.12,
  sort = c("none", "ascending", "descending"),
  sort_metric = c("median_absolute_residual", "mean_absolute_residual", "mean_residual")
)

plot_validate_flow_scatter(...)
}
\arguments{
\item{residuals}{A data frame, one \code{validate_flow_residuals()} result, or a
named list of \code{validate_flow_residuals()} results.}

\item{comparisons}{Comparisons to plot. Defaults to
\code{"adjusted_vs_benchmark"}. Use a character vector to compare multiple
flow-pair definitions. When \code{"raw_vs_benchmark"} is included, it is shown
once as the unadjusted raw MPD baseline.}

\item{methods}{Optional character vector of method identifiers or display
labels to include. Default \code{NULL} includes all methods.}

\item{method_col}{Column containing the method identifier. Default
\code{"method"}.}

\item{method_labels}{Optional named character vector used to relabel methods.}

\item{difference_limits}{Optional numeric vector of length 2 giving the
colour-scale limits for the signed difference. Values outside the limits
are squished to the end colours.}

\item{difference_quantile}{Quantile of absolute signed differences used for
robust symmetric colour limits when \code{difference_limits = NULL}. Set to \code{1}
to use the full observed range. Default \code{0.95}.}

\item{white_band}{Width of the neutral white band around zero as a proportion
of the colour scale. Increase this to make near-zero differences read as
neutral. Default \code{0.22}.}

\item{point_alpha}{Point opacity. Default \code{0.45}.}

\item{point_size}{Point size. Default \code{1.1}.}

\item{point_outline}{Logical; draw points as filled circles with a subtle
neutral outline. Default \code{TRUE}.}

\item{point_outline_colour}{Outline colour used when \code{point_outline = TRUE}.
Default \code{"#6B7280"}.}

\item{point_stroke}{Outline stroke width used when \code{point_outline = TRUE}.
Default \code{0.12}.}

\item{sort}{Method sort order. Use \code{"none"} to keep the supplied method
order, \code{"ascending"} for smallest-to-largest values, or \code{"descending"} for
largest-to-smallest values. The unadjusted raw MPD baseline remains at the
right. Default \code{"none"}.}

\item{sort_metric}{Residual summary used when \code{sort} is not \code{"none"}.
Options are \code{"median_absolute_residual"}, \code{"mean_absolute_residual"}, and
\code{"mean_residual"}. Default \code{"median_absolute_residual"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds faceted scatterplots from \code{validate_flow_residuals()} data. The
default comparison is adjusted versus benchmark, with colour showing the
signed difference between the second and first named series. For a single
comparison, axis labels name the selected X and Y series directly; for
multiple comparisons, facet headers include the compared series.
}
