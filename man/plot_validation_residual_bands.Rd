% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_validation.R
\name{plot_validation_residual_bands}
\alias{plot_validation_residual_bands}
\alias{plot_validate_flow_residual_heatmap}
\title{Plot residual outlier bands as stacked bars}
\usage{
plot_validation_residual_bands(
  residuals,
  method_col = "method",
  method_labels = NULL,
  comparisons = "adjusted_vs_benchmark",
  methods = NULL,
  band_method = c("sd", "quantile"),
  sd_reference = c("selected", "adjusted", "pooled"),
  quantile_reference = c("selected", "adjusted", "pooled"),
  quantile_probs = c(0.5, 0.75, 0.9, 0.95),
  palette = NULL,
  label_min_share = 8,
  orientation = c("horizontal", "vertical"),
  sort = c("none", "ascending", "descending"),
  sort_metric = c("severe_share", "mean_band_score")
)

plot_validate_flow_residual_heatmap(...)
}
\arguments{
\item{residuals}{A data frame, one \code{validate_flow_residuals()} result, or a
named list of \code{validate_flow_residuals()} results.}

\item{method_col}{Column containing the method identifier. Default
\code{"method"}.}

\item{method_labels}{Optional named character vector used to relabel methods.}

\item{comparisons}{Flow comparison(s) to plot. Defaults to
\code{"adjusted_vs_benchmark"}. Use \code{"all"} to facet all supported comparisons.
When \code{"raw_vs_benchmark"} is included, it is shown once as the unadjusted
raw MPD baseline.}

\item{methods}{Optional character vector of method identifiers or display
labels to include. Default \code{NULL} includes all methods.}

\item{band_method}{Banding strategy. \code{"sd"} uses standard-deviation bands;
\code{"quantile"} uses shared absolute-residual quantile cut points. Default
\code{"sd"}.}

\item{sd_reference}{Standard-deviation reference. \code{"selected"} uses the
selected comparison residuals, \code{"adjusted"} uses the pooled
benchmark-minus-adjusted residuals, and \code{"pooled"} uses all three canonical
comparison residuals. Used when \code{band_method = "sd"}.}

\item{quantile_reference}{Quantile reference. \code{"selected"} uses the selected
comparison residuals, \code{"adjusted"} uses pooled benchmark-minus-adjusted
absolute residuals, and \code{"pooled"} uses all three canonical comparison
residuals. Used when
\code{band_method = "quantile"}.}

\item{quantile_probs}{Interior quantile cut points used when
\code{band_method = "quantile"}. Default \code{c(0.5, 0.75, 0.9, 0.95)}.}

\item{palette}{Optional named or unnamed character vector of colours.
Defaults to a colour-blind-aware sequential palette ordered from lower to
higher residual-severity bands.}

\item{label_min_share}{Minimum segment share, in percentage points, required
for an in-bar label. Default \code{8}.}

\item{orientation}{Bar orientation. \code{"horizontal"} places methods on the
y-axis and shares on the x-axis; \code{"vertical"} keeps methods on the x-axis.
Default \code{"horizontal"}.}

\item{sort}{Method sort order. Use \code{"none"} to keep the supplied method
order, \code{"ascending"} for smallest-to-largest values, or \code{"descending"} for
largest-to-smallest values. The unadjusted raw MPD baseline remains at the
bottom for horizontal bars and at the right for vertical bars. Default
\code{"none"}.}

\item{sort_metric}{Residual-band summary used when \code{sort} is not \code{"none"}.
\code{"severe_share"} sorts by the share in the highest residual band;
\code{"mean_band_score"} sorts by the share-weighted residual-band score.
Default \code{"severe_share"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Summarises absolute flow-comparison differences into residual
standard-deviation or quantile bands and plots the share of OD pairs in each
band. The default comparison is adjusted versus benchmark.
}
