% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_flow_methods.R
\name{validate_flow_prepare_output}
\alias{validate_flow_prepare_output}
\alias{validate_flow_prepare_outputs}
\title{Prepare adjusted flow outputs for validation}
\usage{
validate_flow_prepare_output(
  adj_df,
  mpd_df,
  flow_col_mpd = "flow",
  flow_col_adj = "flow_adj"
)

validate_flow_prepare_outputs(
  adjusted_outputs,
  mpd_df,
  flow_col_mpd = "flow",
  flow_col_adj = "flow_adj"
)
}
\arguments{
\item{adj_df}{Data frame with adjusted flows.}

\item{mpd_df}{Raw MPD flow data with \code{origin}, \code{destination}, and the raw
flow column.}

\item{flow_col_mpd}{Name of the raw MPD flow column in \code{mpd_df}. Default
\code{"flow"}.}

\item{flow_col_adj}{Name of the adjusted-flow column in \code{adj_df}. Default
\code{"flow_adj"}.}

\item{adjusted_outputs}{Named list of adjusted-flow data frames.}
}
\value{
\code{validate_flow_prepare_output()} returns a tibble. If \code{adj_df}
already contains a \code{flow} column, that column is retained as
\code{adjusted_input_flow} and the raw MPD flow from \code{mpd_df} becomes \code{flow}.
\code{validate_flow_prepare_outputs()} returns a named list of prepared tibbles.
}
\description{
\code{validate_flow_prepare_output()} joins an adjusted-flow table to the raw MPD
flow column used by validation comparisons. \code{validate_flow_prepare_outputs()}
applies the same preparation to a named list of adjusted-flow tables.
}
\details{
These helpers are useful when outputs come from different adjustment routes
or cached files but need the common \code{debiasR} validation columns:
\code{origin}, \code{destination}, \code{flow}, and \code{flow_adj}.
}
