% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_validation.R
\name{validate_flow_plot_metrics}
\alias{validate_flow_plot_metrics}
\title{Plot overall validation metrics by adjustment method}
\usage{
validate_flow_plot_metrics(
  metrics,
  error_measures = c("mae", "rmse", "mape"),
  metric_cols = NULL,
  comparisons = "adjusted_vs_benchmark",
  methods = NULL,
  method_col = "method",
  method_family_col = NULL,
  method_labels = NULL,
  palette = NULL,
  relative_error_breaks = seq(0, 100, by = 10),
  sort = c("none", "ascending", "descending"),
  sort_metric = NULL
)
}
\arguments{
\item{metrics}{A data frame, one \code{validate_flow_overall()} result, or a named
list of \code{validate_flow_overall()} results.}

\item{error_measures}{Character vector of error measure IDs or labels to
plot. Defaults to \code{c("mae", "rmse", "mape")}. Common labels such as
\code{"MAE"}, \code{"RMSE"}, \code{"MAPE"}, and \code{"Mean absolute error"} are accepted.}

\item{metric_cols}{Compatibility alias for \code{error_measures}.}

\item{comparisons}{Flow comparison(s) to plot. Defaults to
\code{"adjusted_vs_benchmark"}. Use \code{"all"} when \code{metrics} contains rows for
all supported comparisons. When \code{"raw_vs_benchmark"} is included, it is
shown as a single unadjusted raw MPD baseline row.}

\item{methods}{Optional character vector of method identifiers or display
labels to include. Default \code{NULL} includes all methods.}

\item{method_col}{Column containing the method identifier. Default
\code{"method"}.}

\item{method_family_col}{Optional column containing method-family labels.
Retained for compatibility with earlier prototypes; the metric matrix is
organised by method and metric.}

\item{method_labels}{Optional named character vector used to relabel methods.}

\item{palette}{Optional character vector of colours for the relative-error
fill scale. Defaults to a yellow-green-blue sequence suitable for ordered
error intensity.}

\item{relative_error_breaks}{Numeric percentage cut points used to bin the
within-metric relative-error fill scale. Default \code{seq(0, 100, by = 10)}
produces legend ranges such as 0-10, 11-20, and 91-100.}

\item{sort}{Method sort order. Use \code{"none"} to keep the supplied method
order, \code{"ascending"} for smallest-to-largest values, or \code{"descending"} for
largest-to-smallest values. The unadjusted raw MPD baseline remains at the
bottom of the matrix. Default \code{"none"}.}

\item{sort_metric}{Metric used when \code{sort} is not \code{"none"}. Defaults to the
first plotted metric.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds a method-by-metric matrix from \code{validate_flow_overall()} results or a
data frame containing one row per method. Cell fill is scaled within each
metric so unlike metrics are not forced onto a common raw scale, while cell
labels show the original metric values.
}
