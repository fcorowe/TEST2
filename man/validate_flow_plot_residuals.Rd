% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_validation.R
\name{validate_flow_plot_residuals}
\alias{validate_flow_plot_residuals}
\title{Plot validation flow-difference distributions as violins}
\usage{
validate_flow_plot_residuals(
  residuals,
  residual = c("signed", "absolute", "percent"),
  comparisons = "adjusted_vs_benchmark",
  methods = NULL,
  method_col = "method",
  method_labels = NULL,
  palette = NULL,
  show_points = TRUE,
  show_counts = TRUE,
  point_alpha = 0.25,
  point_size = 0.6,
  jitter_width = 0.12,
  violin_alpha = 0.5,
  max_points = 5000,
  y_transform = c("pseudo_log", "identity"),
  sort = c("none", "ascending", "descending"),
  sort_metric = c("median_absolute_residual", "mean_absolute_residual", "mean_residual")
)
}
\arguments{
\item{residuals}{A data frame, one \code{validate_flow_residuals()} result, or a
named list of \code{validate_flow_residuals()} results.}

\item{residual}{Difference scale: \code{"signed"}, \code{"absolute"}, or \code{"percent"}.}

\item{comparisons}{Comparisons to plot. Defaults to
\code{"adjusted_vs_benchmark"}. Use a character vector to compare multiple
flow-pair definitions. When \code{"raw_vs_benchmark"} is included, it is shown
once as the unadjusted raw MPD baseline.}

\item{methods}{Optional character vector of method identifiers or display
labels to include, giving users control over which adjustment methods are
compared. Default \code{NULL} includes all methods.}

\item{method_col}{Column containing the method identifier. Default
\code{"method"}.}

\item{method_labels}{Optional named character vector used to relabel methods.}

\item{palette}{Optional character vector of colours. Defaults to a bright
colour-blind-aware categorical palette inspired by the shared validation
visual style.}

\item{show_points}{Logical; show jittered OD-pair values over each violin.
Default \code{TRUE}.}

\item{show_counts}{Logical; show the number of plotted OD pairs above each
violin. Default \code{TRUE}.}

\item{point_alpha}{Point opacity. Default \code{0.25}.}

\item{point_size}{Point size. Default \code{0.6}.}

\item{jitter_width}{Horizontal jitter width for OD-pair points. Default
\code{0.12}.}

\item{violin_alpha}{Violin fill opacity. Default \code{0.5}.}

\item{max_points}{Maximum number of OD-pair points to draw per
method-comparison group. The violin density and count labels still use all
rows. Use \code{Inf} to draw every point. Default \code{5000}.}

\item{y_transform}{Y-axis transformation. \code{"pseudo_log"} gives a log-like
display that can still show zero and negative signed residuals. Use
\code{"identity"} for the raw residual scale. Default \code{"pseudo_log"}.}

\item{sort}{Method sort order. Use \code{"none"} to keep the supplied method
order, \code{"ascending"} for smallest-to-largest values, or \code{"descending"} for
largest-to-smallest values. The unadjusted raw MPD baseline remains at the
right. Default \code{"none"}.}

\item{sort_metric}{Residual summary used when \code{sort} is not \code{"none"}.
Options are \code{"median_absolute_residual"}, \code{"mean_absolute_residual"}, and
\code{"mean_residual"}. Default \code{"median_absolute_residual"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Builds violin plots from \code{validate_flow_residuals()} data. Differences are
aligned with the scatterplot convention used by
\code{validate_flow_plot_scatter()}: positive values mean the second named series is
larger than the first named series. The default style uses translucent
violins, jittered OD-pair points, and sample-size labels above each method.
}
